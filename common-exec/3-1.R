#ER
degree(ER) # 次数中心性
closeness(ER) #近接中心性
betweenness(ER) # 媒介中心性
#BA
degree(BA) # 次数中心性
closeness(BA) #近接中心性
betweenness(BA) # 媒介中心性
#WS
degree(WS) # 次数中心性
closeness(WS) #近接中心性
betweenness(WS) # 媒介中心性
